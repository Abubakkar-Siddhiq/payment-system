server:
  port: 8080

spring:
  application:
    name: gateway

  cloud:
    gateway:
      server:
        webflux:
          routes:
            - id: auth-service
              uri: http://localhost:8081
              predicates:
                - Path=/api/v1/auth/**
              filters:
                - StripPrefix=2

            - id: payment-service
              uri: http://localhost:8082
              predicates:
                - Path=/api/v1/payments/**,/api/v1/accounts/**
              filters:
                - StripPrefix=2

            - id: fraud-service
              uri: http://localhost:8083
              predicates:
                - Path=/api/v1/fraud/**
              filters:
                - StripPrefix=2

jwt:
  secret: 2Qj3ZpP1QqWn5J6c6h1s7Q9sQ3k8Xb9mF4yJc0rP2tU=

api_prefix: "/api/v1"

logging:
  level:
    org.springframework.cloud.gateway: TRACE
    org.springframework.http.server.reactive: DEBUG
    org.springframework.web.reactive: DEBUG

management:
  tracing:
    sampling:
      probability: 1.0  # 100% sampling for development
  otlp:
    metrics:
      export:
        url: http://localhost:4318/v1/metrics
  opentelemetry:
    tracing:
      export:
        otlp:
          endpoint: http://localhost:4318/v1/traces
    logging:
      export:
        otlp:
          endpoint: http://localhost:4318/v1/logs